package example14_lambda_expression;

// Использование ссылки на метод экземпляра для
// обращения к любому интерфейсу
public interface MyIntNumPredicate {
    public boolean test(MyIntNum mv, int n);

}
